language: c

install:
 - sudo apt-get install mono-gmcs cli-common-dev > /dev/null

script:
 - xbuild /p:Configuration=Release

# builds
 - rm -rf out || exit 0;
 - mkdir out;
 - cd out
 - git init
 - git config user.name "Travis-CI"
 - git config user.email "travis@example.com"
 - cp -r ../Output ./Output
 - git add .
 - git commit -m "Deployed to Github Pages"
 - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:gh-pages > /dev/null 2>&1

env:
  global:
  - GH_REF: github.com/manicdigger/EnetCito-builds
  - secure: "gmkmcWfyZ8Nssp2hJX+3nKkFS/WqeLO7yH7SLdkLezJU7vNoNhd31YbgL+1EEW6r9DLi/1TlX5/AeU3+dSoPF7F2ECD05dfIwX0PZv8Ic8BZ7Nsehkv77iRR+MLr9tN3UIrghq6ilhlLw+bkvmlzBJ8fjT6QyHeY2Worzdk0mJE="
 
